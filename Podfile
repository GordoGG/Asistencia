# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Asistencia' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Asistencia
	
  pod 'Firebase', '~> 6.7'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'Kingfisher', '~> 5.7'

  target 'AsistenciaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AsistenciaUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
